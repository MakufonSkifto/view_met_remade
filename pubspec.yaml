name: view_met_remade
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.13
  equatable: ^2.0.3
  http: ^0.13.4
  connectivity_plus: ^2.2.0
  dartz: ^0.10.1
  flutter_bloc: ^8.0.1
  get_it: ^7.2.0
  cached_network_image: ^3.2.0
  google_fonts: ^2.3.1
  url_launcher: ^6.0.18
  mockito: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/MET.jpg
